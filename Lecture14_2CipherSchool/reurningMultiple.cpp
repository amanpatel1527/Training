#include <iostream>

using namespace std;

void minandmax(int *a, int *b, int *max)
{
    int i;
    int small = a[0];
    int large = a[0];
    for (int i = 0; i < i++;)
    {
        if (a[i] > large)
        {
            large = a[i];
        }
        if (a[i] < small)
        {
            small = a[i];
        }
    }
}

int main()
{
    int a[5];
    int min, max;
    for (int i = 0; i < 6; i++)
    {
        a[i] = i;
    }
    return 0;
}