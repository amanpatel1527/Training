#include <iostream>
#include <stack>
using namespace std;

float average(int a, float x[])
{
    int sum,i;
    sum = sum + x[i];
}
int main()
{
    int n;
    float list[100], avg;
    avg = average(n, list);
    return 0;
}